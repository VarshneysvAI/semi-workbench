# ---- SEMI backend — base (installed in backend/.venv; versions verified on py3.13) ----
fastapi==0.141.1
uvicorn[standard]==0.52.1
pandas==3.0.5
openpyxl==3.1.5
python-multipart==0.0.32
websockets==16.1.1
beautifulsoup4==4.15.0
lxml==6.1.1
httpx==0.28.1
pytest==9.1.1
python-dotenv==1.2.2
google-genai==2.17.0
requests>=2.31.0

# ---- discovery & extraction: SearXNG, DDG, Crawl4AI, PyPDF ----
duckduckgo-search>=6.0.0
crawl4ai>=0.4.0
pypdf>=4.0.0
curl-cffi>=0.7.3

# ---- optional extraction backends (set the matching env to enable) ----
# firecrawl-py        # FIRECRAWL_API_KEY -> PDF/web/YouTube via api.firecrawl.dev
# yt-dlp>=2025.0.0    # YouTube transcripts when no platform subs (free)
# faster-whisper      # local audio transcription (optional, GPU helpful)

# ---- optional model clients (any OpenAI-compatible endpoint) ----
# openai>=1.40.0      # convenience; SEMI uses raw httpx so this is optional

# ---- deferred to robustness sprints (not required for UniHack demo) ----
# playwright==1.46.0 marker-pdf==0.1.0 easyocr==1.7.2
# sentence-transformers==3.0.1 torch==2.4.0 scipy==1.13.1 scikit-learn==1.5.0